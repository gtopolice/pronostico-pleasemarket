# Resolution

Creators who tag @Chiwiwis are on-chain oracle. SLA: `CHIWIWIS_RESOLVE_SLA_HOURS` (default 48) after close.

Reminders: worker posts at close and +24h. After SLA → `platform_escalation` status; org resolution-service owns the case.

Admin: `resolveViaAdmin` / kill via pronostico-apps/admin.
